<UserControl x:Class="ImageTools.ImageRenamer.ImageRenamer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local ="clr-namespace:ImageTools.ImageRenamer"
      mc:Ignorable="d" 
      d:DesignHeight="293.088" d:DesignWidth="523.963">
    <UserControl.Resources>
        <Style TargetType="ListViewItem" x:Key="lisViewStyle">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=ExifInfoReadSuccess}" Value="false">
                    <Setter Property = "Foreground" Value="Red"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="53*" />
            <ColumnDefinition Width="22*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel>
            <StackPanel Orientation="Horizontal" Height="30px">
                <TextBox Width="Auto" MinWidth="100pt" VerticalAlignment="Center" Name="initFolder" Text="{Binding Path=InitDir, Mode=TwoWay}" Margin="0,6" ></TextBox>
                <Button Name="selectFolder" VerticalAlignment="Center" Margin="10px 0 0 0" Padding="10px 0" Click="selectFolder_Click">...</Button>
            </StackPanel>
            <CheckBox Name="useSubFolders" Height="15px" IsChecked="{Binding Path=IncludeSubDirs, Mode=TwoWay}">Include subfolders</CheckBox>
        </StackPanel>
        <ListView Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Name="lv_files" Margin="0 10px 0 0">
            <ListView.ItemTemplate >
                <DataTemplate DataType="local:ImageRenameConfig">
                    <ListViewItem Style="{StaticResource ResourceKey=lisViewStyle}">
                        <Label Content="{Binding Path=OldDisplayValue}" />
                    </ListViewItem>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
        <StackPanel Grid.Column="1" Grid.Row="0">
            <Button Margin="5px" VerticalAlignment="Stretch">Hello</Button>
        </StackPanel>
    </Grid>
</UserControl>
